/** Shared horizontal rhythm. Gutters grow with the viewport; content caps at 1440px. */
export const GUTTER = "px-5 md:px-8 xl:px-12";
export const CONTAINER = "mx-auto w-full max-w-[1440px]";

/** Header height in pixels. Keep in step with `h-16` on the header and `pt-16` on main. */
export const HEADER_HEIGHT = 64;
